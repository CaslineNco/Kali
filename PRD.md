# 年度专注格子墙 — PRD

## 0. 一句话愿景

一个 Windows 桌面 App：把一整年的「工作/学习/创作」专注时间画成 GitHub 贡献图式的格子墙，年底打开能看到自己这一年确实做了事；格子深浅只由专注时长决定，娱乐时间单独统计、不影响颜色。

## 1. 使用者

- 只有一个用户：我自己。不考虑多用户、不考虑账号体系。
- 我不写代码、不读代码。每个开发阶段必须能**只看界面**判断做没做对，不需要看代码或日志。

## 2. 核心设计原则（这些是红线，任何功能设计冲突时以这几条为准）

1. **诚实记录，不做监控**：不采集操作系统活动、不读取窗口标题/进程名、不做任何后台自动判断"你在干嘛"。专注时长由用户自己启停计时器产生，数据是否可信由用户自己负责。
2. **不做游戏化压力**：不做"连续打卡天数（streak）"这种数字、不做"你破坏连续记录了"之类的提醒/通知。格子墙是年底回看用的诚实记录，不是任务打卡系统。
3. **娱乐时间不参与颜色计算**：娱乐时间要能记录、要能看到统计，但格子的深浅只由当天「专注」类型的时长决定，两者在数据层面完全独立。
4. **验收不依赖读代码**：每个阶段的验收标准必须是"打开界面点几下就能确认"，不能是"看日志/看数据库/看代码逻辑"。

## 3. 调研结论（影响本 PRD 的关键判断，来自 Reddit 用户反馈）

- 大量长期用户明确偏好**手动计时**而非自动追踪，核心原因是"信任感"——手动记录的数字是自己认的；自动追踪即使号称精确，也常常"看着准但不完全对"，核对反而比手动记录更费时间。
- 手动记录最大的弱点是"忘记启停"，需要一个轻量的事后补救方式，而不是转向自动监控。
- 已有一个几乎同构的独立项目（Heron, useheron.app）验证了本 PRD 的方向：贡献图式年历 + 不做连续打卡计数/破坏提醒。

**结论：v1 采用纯手动计时，不做任何自动窗口/进程监控。**

## 4. 功能范围（v1 / MVP）

### 4.1 日程规划 + 到点确认（取代"简单计时器"模型）

不是一个孤零零的"开始专注"按钮，而是**当天的日程安排**：

- 可以给当天提前排「时间块」（比如 9:00–11:00 写方案），标类型（专注/娱乐）和内容备注。像参考视频里的日历一样，是"今天打算做什么"的规划视图。
- **时间块到点后，系统提醒确认**："这段时间你做了吗？" —— 用户可以：确认（按计划时长计入）、调整时长（实际做了多久，跟计划不一致时改）、跳过（这段没做，不计入）。
- **只有确认过的时间块，才计入当天的「实际专注时长」（也就是决定格子颜色的数字）**。没确认/跳过的计划块不算数——这是为了守住"格子颜色只反映真的做了多久"这条红线（见第 2 节原则 1），避免"排了计划但没做，格子却变深了"这种造假。
- 同一天可以有多个时间块，当天某类型的总时长 = 当天所有**已确认**的该类型时间块时长之和。
- 提供「补记」功能：不提前排计划、事后直接补一段时长（选日期 + 时长，**精确到分钟**），用于忘了排计划或忘了确认的补救。
- 具体交互细节（怎么排时间块、确认提醒长什么样、托盘图标点击后是什么界面）还没敲定，需要专门讨论，见第 6 节。

### 4.1.1 常驻悬浮窗（取代"托盘小窗口"）

不用点击才能看，而是**一直挂在桌面上的透明小窗**：

- 显示当前时间块 / 接下来的下一个时间块（不是完整时间轴，保持极简、不占地方）。
- 到点需要确认时，**可以直接在悬浮窗上点确认/调整/跳过**，不需要切换到主窗口。
- 半透明、可置顶、不在任务栏显示；具体的位置/大小/是否可拖拽/透明度这些细节待定，见第 6 节。
- 悬浮窗可以关掉/隐藏（不想要的时候），从托盘图标右键菜单里切换显示/隐藏。

### 4.1.2 系统托盘
- App 常驻系统托盘图标（Windows 任务栏右下角，跟其他后台软件一样的位置）。
- 右键菜单：显示/隐藏悬浮窗、打开主窗口、设置、退出。
- 左键点击行为待定（比如直接打开主窗口，还是跟右键菜单一样弹出选项），见第 6 节。

### 4.2 年度格子墙
- GitHub 贡献图式布局：52 列 × 7 行，一天一格，一年一屏。
- 格子颜色深浅仅由当天「专注」总时长（已确认的时间块之和）决定，分档规则待定（见第 6 节待确认问题）。
- 「娱乐」时长不影响颜色，但点击某天格子展开详情时，应能看到：当天专注总时长、当天娱乐总时长、以及当天各时间块的明细（几点到几点、什么内容、专注还是娱乐）。
- 不做连续打卡天数统计、不做"断签"提示。
- 未记录任何时长的日期（0 分钟）与"还没到的未来日期"在视觉上要能区分（避免误以为过去某天是"记录了 0 分钟"还是"这天本来就没数据"）。

### 4.3 主窗口 & 开机自启
- 主窗口用于查看格子墙、当日/日程详情——按需打开，不强制常驻显示。
- 开机自启动：**设置里的开关，默认关闭**，用户自己选择是否开启。绝不做静默自启、卸载残留这类"国产垃圾软件"式行为。

### 4.4 数据存储
- v1：纯本地存储（本地文件/SQLite），不联网、不需要账号。
- 数据结构设计时预留字段/表结构，方便未来加云同步，但 v1 不实现同步功能，也不因此增加当前版本的复杂度。

## 5. 明确不做（v1 Non-goals）

- 不做自动窗口/进程活动监控。
- 不做连续打卡天数（streak）、排行榜、成就徽章等游戏化机制。
- 不做云同步、多设备访问（架构留口子，功能不做）。
- 不做移动端 / 网页端。
- 不做团队协作、多用户。

## 6. 待确认问题

已确认（原第 6 节问题的结论）：
- 补记粒度：精确到分钟。
- 开机自启动：默认关闭，用户可在设置里主动开启，不做静默/强制自启。
- 计划块与实际时长的关系：到点提醒确认（确认/调整/跳过），只有确认过的才计入格子颜色，见 4.1。

还没讨论、会影响后续实施阶段拆分的开放问题：

1. **日程块怎么排**：在主窗口的日历视图里手动拖拽/点击创建时间块？还是有更快捷的方式（比如从托盘直接加一条）？一天大概会排几个块、内容备注要多详细，这些决定了排程界面的复杂度。
2. **到点确认提醒长什么样**：系统通知？App 内弹窗？还是主窗口右上角的一个待确认列表，不打断当前工作？如果当时不在电脑前，提醒堆积了怎么处理（比如一次性批量确认昨天漏确认的几个块）？
3. **托盘图标点击后的界面**：是今日日程速览（今天排了哪些块、哪些还没确认）？还是别的？点开是气泡窗口还是打开主窗口的某个视图？
4. **颜色分档**：GitHub 是 5 档（含"无记录"）。本 PRD 默认沿用「5 档，档位边界按个人常见单日专注时长分布来定，具体数值等几周真实数据后再调」——可以吗，还是想要连续渐变色？

## 7. 视觉与动效（待细化，不阻塞 PRD 其余部分）

- Remotion 和 HyperFrames 是同一类工具：把 HTML/CSS/JS + 动画库（GSAP/Lottie/Three.js 等）离线渲染成**确定性的 MP4 视频**，不是运行时的交互式 UI 引擎。两者选一个即可，不需要都用。
- 因此它们只适合做**不需要跟用户交互、数据已固定的一次性动效**，比如年底"回看这一年"的展示片段、转场特效素材——渲染好之后作为视频/Lottie 资源嵌入 App 里播放。
- 格子墙本体（需要悬停查看当天数据、点击查看详情、颜色随真实数据变化）是**交互式 UI**，必须在 App 的前端框架里直接实现，不能靠 Remotion/HyperFrames 生成。

### 7.1 参考视频确认的交互模式：点格子 → 缩放展开详情

参考视频（`D:\Downloads\用自己的skill做的动效...mp4`，已截图存于 `Img/`）展示的是一个日程类 App 的核心转场：点击年度格子墙上的某一天，整个格子墙带运动模糊缩放进入当天详情，返回时同样缩放回格子墙，某天格子在过程中高亮标记。

已确认采纳的部分：
- **点击某天格子 → 从该格子的位置"展开缩放"进入当天详情页**（显示当天专注/娱乐时长 + 当天各时间块明细），不是生硬的切页/弹窗。
- 返回时同样以缩放动画收回格子墙。

已确认调整的部分：
- **不要参考视频里那么强的运动模糊**。这是每天会高频点开的工具，转场要轻、要快，不能因为动效炫而拖慢日常使用——弱化或去掉运动模糊，保留"从格子展开"的空间连续性即可。

技术实现：这是数据驱动的运行时交互（点哪天展开哪天），要用 Framer Motion 的 layout / shared-element 过渡在 React 里直接实现，不能用 Remotion/HyperFrames 预渲染（那两个工具只产出固定视频，无法响应"用户点了哪一天"）。Remotion/HyperFrames 的定位收窄为：设计阶段做转场效果预览/探索，或做年底回顾之类的固定展示片段——不是这个核心转场的实现方式。

### 7.2 待补充
- 年终回顾展示片段等一次性动效场景，具体做不做、做成什么样，留到"视觉动效"阶段再定，不影响当前功能类阶段开发。

## 8. 技术栈

- 桌面框架：Tauri（Rust 后端 + Web 前端）。
- 前端框架：**React**——Tauri 原生支持、生态成熟、AI 代码生成质量最稳定；格子墙可基于现成的贡献图组件（如 `react-activity-calendar`）改造，不用从零造轮子；交互动效用 Framer Motion（运行时、数据绑定），跟 Remotion/HyperFrames 的"离线渲染"定位互补不冲突。
- 目标平台：Windows（v1 只做 Windows，不考虑 macOS/Linux）。
- 本地存储：待定（SQLite 或本地文件），第 4.4 节所述。
