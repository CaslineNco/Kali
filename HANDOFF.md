# Handoff — 年度专注格子墙

给新窗口/新会话看的：这个项目现在到哪一步了、东西都在哪、接下来具体做什么。

## 这是什么

Windows 桌面 App，Tauri + React。把一年的「专注」时间画成 GitHub 贡献图式的格子墙，娱乐时间单独记不影响颜色。完整产品定义看 **`PRD.md`**（这份 handoff 不重复 PRD 里已经写清楚的东西，只讲"现状"和"接下来做什么"）。

用户（产品的唯一使用者）**不写代码、不读代码**，每个阶段的验收都必须是"打开界面点几下就能确认"，不能要求看代码/日志/数据库。这条贯穿所有阶段，写代码的人自己要记住。

## 现状

- **PRD.md**：完整产品定义、核心原则、功能范围、待确认问题、配色方案，已经比较稳定。
- **Figma 线稿**（低保真，7 个屏幕）：https://www.figma.com/design/nX7h4fQFKtC4hK6GODOdck
  年度格子墙 / 日程视图 / 常驻悬浮窗 / 新建时间块 / 补记 / 设置 / 本周固定安排。配色已统一（暖色调深色主题，见 PRD 9.9），交互逻辑对应 PRD 第 9 节逐屏说明。
- **GitHub 仓库**（public）：https://github.com/CaslineNco/Kali
- **`ref/UI/` 里的参考组件**（beui.dev / bencho.dev，MIT license）已经在 Figma 线稿里落地对应关系，见 PRD 9.8：
  - `Heat Calender.md` → 年度格子墙（`npx shadcn add @beui/heat-calendar`）
  - `Todo List.md` + `Swipeable List.md` → 常驻悬浮窗
  - `Expandable Action Bar.md` → 日程视图底部操作栏
  - `Availability Scheduler.md` → 本周固定安排
  - `Stepper.md` / `Setting timer.md`（bencho.dev，无正式文档，只有效果参考）→ 补记的时长圆盘

## 还没定的问题（PRD 第 6 节）

开始写功能代码前最好先定，不然会返工：

1. 日程块怎么排（拖拽创建？多快捷？）
2. 到点确认提醒的形式（系统通知/App 内/列表式，不在电脑前时怎么补处理堆积的待确认）
3. 托盘左键点击行为
4. 格子颜色阈值的具体数值（先用 PRD 9.9 的默认阶梯，攒真实数据后再调，不阻塞开发）

3、4 不阻塞开发（有合理默认值），1、2 建议开工前先拍板，因为直接决定数据模型（时间块的状态机）和至少两个界面的交互实现。

## 技术栈（已定，见 PRD 第 8 节）

- Tauri（Rust 后端 + React 前端）
- Framer Motion 做格子墙 ↔ 日程视图的缩放转场（运行时交互，不能用 Remotion/HyperFrames 预渲染，见 PRD 7.1）
- 本地存储：SQLite 或本地文件，待定，不影响现在开工
- Windows only，v1 不考虑其他平台

## 接下来：分阶段实施计划

每个阶段的验收标准都写成"打开界面能看到/点到什么"，不是"代码逻辑对不对"。

**阶段 0 — 项目脚手架**
产出：Tauri + React 项目能跑起来，双击图标弹出一个空窗口。
验收：桌面上有个图标，点开有个窗口，窗口没崩。

**阶段 1 — 年度格子墙（假数据，纯展示）**
产出：用 Heat Calendar 组件渲染 52×7 格子墙，数据先写死（假造一年的随机时长），配色照 PRD 9.9。
验收：打开 App 直接看到格子墙，颜色深浅肉眼可辨（不是看起来都一样），风格跟 Figma 线稿对得上。

**阶段 2 — 本地数据层**
产出：时间块的数据表/存储结构落地（日期、类型、开始/结束时间、状态、备注），先不连 UI，写几条测试数据进去能读出来。
验收：这阶段用户看不到界面变化，验收方式是让开发者口头/文档说明数据能存能读，允许跳过"界面验收"这条（唯一例外，纯基础设施）。

**阶段 3 — 日程视图 + 新建时间块 + 补记（先不做到点确认）**
产出：日程视图三栏轮播能看某一天的时间块；新建时间块、补记两个浮层能真的建出数据，存进阶段 2 的数据层，日程视图里能看到。
验收：点格子墙某天 → 进日程视图；点"新建时间块"排一个块 → 真的出现在时间轴上；点"补记" → 真的多一条已确认记录；翻页能看前一天/后一天。

**阶段 4 — 到点确认流程**
产出：时间块到点后状态变"待确认"，提供确认/改时长/跳过操作（具体提醒形式取决于上面"还没定的问题 2"）。
验收：手动造一个"已经到点"的测试块，能在界面上看到它变成待确认状态，点确认/跳过后状态和格子墙颜色跟着变。

**阶段 5 — 常驻悬浮窗**
产出：Todo List + Swipeable List 风格的悬浮窗，显示今天的时间块清单，左右滑确认/跳过。
验收：悬浮窗一直挂在桌面上（不点它也看得到），滑动能操作，跟主窗口的数据是同一份（这边确认了那边也更新）。

**阶段 6 — 系统托盘 + 开机自启**
产出：托盘图标 + 右键菜单（显示/隐藏悬浮窗、打开主窗口、设置、退出）；设置里的开机自启开关。
验收：任务栏右下角有图标；右键菜单每一项都能点得通；开机自启默认关闭，打开后重启电脑能验证（或至少设置项本身能保存状态）。

**阶段 7 — 设置页**
产出：颜色阈值调整、开机自启开关（阶段 6 已做）、悬浮窗透明度/显隐、数据路径展示。
验收：改颜色阈值，格子墙颜色分布跟着变；改悬浮窗透明度，悬浮窗跟着变。

**阶段 8 — 格子墙 ↔ 日程视图缩放转场**
产出：点格子墙某天，用 Framer Motion 做"从格子展开"的转场（弱运动模糊），返回同理。
验收：点击有连续的展开动画，不是生硬跳转；每天会用的功能，动画要快，不能等。

**阶段 9 — 本周固定安排（批量排计划）**
产出：Availability Scheduler 风格的每周编辑器，铺满一周生成多个"计划中"状态的时间块。
验收：勾几天、排个时段、点"铺满这一周"，日程视图对应的接下来 7 天里真的出现了这些块（状态是"计划中"，不是"已确认"）。

**阶段 10 — 打磨**
产出：视觉细节校对（对照 Figma 线稿）、边界情况（连续多天没记录、跨年、时间块重叠）、年终回顾动效（如果要做，见 PRD 7.2，用 Remotion/HyperFrames 预渲染）。
验收：按 Figma 线稿逐屏比对，边界情况手动造数据测试。

---

每个阶段结束，开发者应该主动给用户看一遍界面效果，等用户确认"这阶段可以了"再进入下一阶段——不要因为"代码跑通了"就默认过关，验收标准是用户说了算。
